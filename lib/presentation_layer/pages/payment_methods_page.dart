import 'package:flutter/material.dart';

class PaymentsMethodsPage extends StatelessWidget {
  const PaymentsMethodsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
